
# WHAT I LEARNT


# What is a Struct?
A struct is like a box that can hold different types of stuff.

Unlike arrays (same type only), structs can hold different types in one variable.
Useful for grouping related data (like a person’s profile).